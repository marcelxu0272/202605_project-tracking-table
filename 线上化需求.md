# 项目追踪表线上化 — 需求文档

> **关联文档：** [字段字典.md](./字段字典.md) | [会议记录精修版](./产值报告线上化讨论_精修版.md) | [关键要点](./产值报告线上化讨论_关键要点.md)  
> **原始数据：** `S520_金山中心_项目执行跟踪详细数据2026年05月.xlsx`  
> **最后更新：** 2026-05-20（存量指标 R/S 预警与完成额校验；平台数据批量同步与底栏更新时间；月度列时间窗等）

---

## 1. 角色定义

系统共设 **6 个角色**，权限分工如下：

| 角色 | 定位 | 编辑权限 | 审批权限 | 说明 |
|---|---|---|---|---|
| **系统管理员** | 系统运维 | ✅ | △ | 负责系统配置、用户管理、权限分配、数据锁定/解锁、报表生成配置等；**填报表格**页可编辑全公司项目（**月度列**仍受 **§2.8.5** 时间窗约束）；工具栏「**更新系统数据**」（§2.2）、「**提交归档**」生成 **J版**（§2.15）；底栏纵览 **12 个板块**进度（§2.15）；开发环境「重置为初始状态」（§2.10）。 |
| **财务审核** | 数据校验 | ❌ | ❌ | **始终只读**：可全公司查看数据看板、填报表格、审批流程、审计日志（无期限限制）；**不可**在线编辑、导入或保存。报告月**次月 1–3 日**为**财务核查提醒期**（界面横幅提示核对开票/回款/预收款等），不改变填报锁定规则。 |
| **板块管理员** | 数据管理 | ✅ | ❌ | 负责板块内数据汇总校验、维护 CRB/财务数据映射关系、异常数据处理。 |
| **项目经理** | 数据填报 | ✅ | ❌ | 负责本人管辖项目的产值、进度、WIP 分析填报，Excel 导入/导出。 |
| **板块总监** | 审批决策 | △ | ✅ | 审批流程页查看本板块全部项目（**§2.14**）；**待办节点**（板块已提交、总监初审中）内可编辑与板块管理员相同的填报列并写入变更批注。 |
| **项目群群主** | 审批决策 | △ | ✅ | 同板块总监；**待办节点**为群主复审（`approve1`）时可编辑可编辑列。 |

---

## 2. 核心功能需求

### 2.1 填报与交互模式
- **在线填报：** 支持在网页端直接修改单元格数据（基于 **Luckysheet**，类 Excel）。
- **填报视图（当前实现）：** 填报页**仅展示 Luckysheet**，为默认且唯一可见的填报界面；**经典 HTML 表格**实现代码保留于 `ProjectEditor.js`，通过 `showLegacyHtmlTable` 开关控制（默认 `false`，UI 不提供切换入口），便于开发对照或临时启用。
- **工具栏视图筛选：** 「全部 / 新增 / 有变更 / **预警**」位于 Luckysheet 上方**表格工具栏**（`.sheet-toolbar`，见 **§2.12.1**），与 **§2.7** 联动；与「填报视图」切换无关。
- **Excel 导入/导出：**
  - 支持下载模板（包含已锁定的基础数据列）。
  - 支持用户上传填好的 Excel，系统自动解析更新。
  - **权限控制：** 无论是线上还是 Excel，均需实现**细粒度的单元格权限控制**（只读 vs 可编辑）。
  - **填报页 vs 管理页导入（已实现）：** 管理设置页为**全量覆盖**项目库；填报页「上传导入」为**按项目号合并**，仅覆盖当前角色可编辑字段（见 **§2.12**）。
- **信息折叠与排序：**
  - 基本信息等辅助信息可折叠，减少视觉干扰。
  - **排序优化：** 当月有数据变动（工时/产值/开票/回款）的项目自动置顶，无变动项目置后。

### 2.2 数据同步与刷新机制
- **滞后可接受：** 跟踪表全量项目范围来自中台/CRB/财务，但表格**不实时**跟随平台变化。以 **`project_no` 为唯一索引**，项目只增不删，手工填报与变更批注不断链；表格内容与平台信息存在滞后属正常情况。
- **批量刷新（已实现框架，开发期 stub）：**
  - **自动同步：** 每日定时（默认本地 **02:00**，`periodConfig.platformSyncHour` 可配）从中台拉取 CRB/财务数据并合并入库；**25 日锁定后暂停**自动任务。
  - **手动同步：** 系统管理员在填报表格工具栏点击「**更新系统数据**」，二次确认后立即刷新；锁定期间仍可手动触发。
  - **合并规则：** 仅覆盖 `system_sync` 字段 + 报告月**当月**开票/回款实际值；**不**覆盖 `manual_input`、`_field_change_log`、报告月**之后**的预测开票/回款；平台新增项目号 → INSERT。
  - **审计：** 每次同步写入 `audit_log`（`operation_type: platform_sync`），**不**写入单元格变更高亮（避免与 PM 手工变更混淆）。
  - **后续：** 真实中台 API 就绪后替换 `server/platform-sync-stub.js`（环境变量 `PTRACK_PLATFORM_API_URL`）。
  - **接口（已实现）：** `POST /api/admin/sync-platform-data`（系统管理员手动触发）；`meta.systemDataSyncedAt` / `systemDataSyncMeta` 经 `GET /api/bootstrap` 下发。
- **最后更新时间（已实现）：** 填报页底栏「共 xx 条记录」**行末**展示 `systemDataSyncedAt`（`系统数据更新于 yyyy-MM-dd HH:mm`）；**所有角色可见**；未同步时显示「尚未同步」。
- **锁定期管理：**
  - 填报期开启时，管理员可**手动锁定数据快照**。
  - 锁定期间（含每月 25 日起），**暂停自动**平台同步；手动同步仍仅系统管理员可用。
  - 填报结束审批后，解锁并可在次月开启新一轮填报前更新数据。

### 2.3 自动化报表
- **WIP 分析报告：** 每月自动生成 PDF 格式的 WIP 分析报告（账龄分析、风险预警、改进建议）。
- **分层输出：** 报告按板块级、项目群级、公司级分别生成。
- **预警通知：** 当 **WIP > 合同额 50%** 时，系统自动向对应中心和板块发送预警。

### 2.4 填报周期与锁定机制 (动态可配置)
- **1 日～3 日：财务核查提醒期**（报告月次月 1–3 日，可配置）：系统向全员展示**提醒**（侧栏/填报页横幅），督促财务核对开票、回款、预收款等数据；**不**单独切换锁定状态，**不**赋予财务编辑权限。填报窗口仍按下方「25 日锁定 / 次月 9 日解禁」及管理员手动锁定规则执行；`lockStatus` 仅 `open` | `locked`（历史 `finance_only` 在启动时归一为 `open`）。
- **19日：填报提醒** 系统自动发送提醒邮件/消息，督促各角色在平台完成当月数据填写。
- **25日：月度锁定** 填报窗口关闭，数据冻结。除系统管理员外，任何人不可编辑（系统管理员仍受 **§2.8.5** 月度列时间窗约束）。
  - *管理员特权：* 系统管理员在锁定期可临时修改**允许编辑范围内**的字段（如 WIP 分析、未来月预测等），操作全程留痕；**不可**在线改写报告月当月及之前的开票/回款实际值列。
- **次月9日：常规解禁** 锁定解除。板块管理员与项目经理可随时修改「可编辑权限」内且符合 **§2.8.5** 时间窗的字段。
- **数据初始化规则 (每月开启填报时自动执行)**：
  - **完成额（当月）**：报告月对应月度完成列（AV–BG 中当月格）自动重置为 `0`，等待 PM 手工填报。
  - **开票/回款（当月）**：报告月对应开票/回款列由**财务系统同步实际发生值**，单元格只读，不在填报期手工覆盖。
  - **历史保留**：报告月**之前**的月度完成/开票/回款均为只读历史数据。
  - **未来预测**：报告月**之后**的开票/回款列为预测值，可继承上一轮填报并允许编辑修改。
- **动态调整：** 上述三个时间节点（19/25/9）必须在系统后台支持**动态配置**，以应对节假日或临时调整。

### 2.5 数据变更追踪 (Audit Log)
- **全量记录：** 任何数据的增、删、改（含在线编辑、Excel 导入、管理员解锁修改）均自动记录变更日志。
- **日志维度：** 操作人、操作时间、所属项目、修改字段、原值、新值、修改原因（可选）。
- **审计支持：** 支持按项目、人员、时间范围查询导出，满足财务与运营审计要求。
- **项目级变更元数据（已实现）：** 除全局 `audit_log` 表外，每个项目对象可携带：
  - `_changed_fields`：本月有变更的字段列号列表（用于高亮与「仅看变更」筛选）；
  - `_field_change_log`：按列号索引的**变更记录数组** `[{ oldVal, newVal, roleLabel, userName, userId, at }, …]`；同一流程内**不同角色**对同一单元格的多次修改**按序追加**不覆盖；**同一角色、同一旧值→新值**的重复记录会去重（见 **§2.7**），并随 `PUT /api/projects/:projectNo` 持久化于 SQLite `projects.payload`。
- **批注与审计的关系：** 单元格批注优先读 `_field_change_log`（展示前经 `dedupeChangeLogList`）；若无记录则回退至 `audit_log` 中该项目、该字段最近一条。全局 `audit_log` 仍保留字段名、时间等完整维度，与批注文案分离。

### 2.6 审批流程与版本快照 (Snapshot Management)
每月填报数据提交后，进入多节点审批流，每次状态变更自动生成**月度快照版本**。

> **分板块 vs 公司归档（已实现，详见 §2.15）：** 板块管理员「提交审批」、总监/群主审批按 **`unit_code`（执行单位编码）** 分板块独立推进；快照键为 `Draft:<板块编码>`、`Approve1:<板块编码>`、`Approve2:<板块编码>`（如 `Draft:SAS520`）。历史 Excel 中的 `S520` 在系统内归一为 **`SAS520`（金山中心）**。**J 版**为系统管理员执行的**全公司一张表**归档（`POST /api/company/archive` → 全局 `J版`），**不要求**全部板块均完成 Approve2（已实现）。

1. **提交生成 Draft 版**：各板块完成填报并提交 -> 生成该板块 `Draft:<板块编码>` 快照，并设置该板块 `reportingSubmitted = true`。
2. **总监审批生成 Approve1 版**：板块总监审核通过 -> 生成 `Approve1:<板块编码>` 快照。
3. **群主审批生成 Approve2 版**：项目群群主审核通过 -> 生成 `Approve2:<板块编码>` 快照；该板块 `approvalStatus` 为 `approve2` 时，流程状态文案为「**已完成审批**」（非「待公司归档」）。
4. **管理员确认生成 J 版**：系统管理员在**填报表格**工具栏点击「**提交归档**」（或调用公司归档 API）-> 生成全局 `J版` 快照（含**全部项目**）；与各板块 Approve2 进度**解耦**。
- **版本对比：** 支持任意两个版本之间的数据差异对比（Diff），支持高亮显示变动单元格（**板块总监 / 项目群群主**审批页不提供，见 **§2.14**）。
- **填报页历史版本预览（已实现）：** 在填报表格工具栏选择「当前填报」或历史快照，**在 Luckysheet 区域内只读展示**该版本数据（见 **§2.12**）。
- **驳回机制（已实现）：** **板块总监**、**项目群群主**在待办节点可「驳回」；`approvalStatus` 恢复为 `draft`，`reportingSubmitted = false`，流程退回**板块管理员**修改并重新「提交审批」。驳回原因可选写入审计日志。
- **审批决策角色专用界面：** 见 **§2.14**。

### 2.7 变更高亮机制 (Diff Highlighting)
为提升填报与审批效率，系统需自动识别并视觉强化本月变动数据：
- **🆕 新增项目整行高亮（已实现）：** 对比**上一报告月归档快照**（`Month:YYYY-MM`，如报告月 `2026-05` 对应 `Month:2026-04`），当前库中**项目号不在上月快照内**的行标记 `_added_this_month = true`；**该行只读列**使用浅品牌色底（Luckysheet `luckysheetCellBg` / 图例「本月新增项目」）。若该格同时属于当前角色**可编辑列**，则按下方「可编辑列」浅黄底显示（见叠色优先级）。
  - **快照生成：** 管理设置 →「生成上月对比快照」，或 `npm run seed:prior-month [剔除条数]` / `POST /api/admin/seed-prior-month-snapshot`（**默认剔除 5 条**项目，其余按 4 月报告月重算公式后写入快照）。
  - **新增行底色：** `#d9e7d8`（`--color-new-project-bg`，与图例一致）。
  - **版本下拉：** `Month:2026-04` 显示为「2026年4月归档 · 时间」，可只读预览上月数据。
- **✏️ 可编辑列整列底色（已实现）：** 在当前角色、锁定规则下**可填报**的字段（`FieldConfig.canEdit` 且 `canEdit`），其**数据格**使用浅黄底，与「本月有变更」的浅橙底区分；表头仍为浅黄底 + 深黄字（见 **§2.8.3**）。
  - **数据格底色：** `#fefce8`（`--color-editable-cell-bg` / `ChangeMeta.EDITABLE_FIELD_STYLE`）。
  - **范围：** 按**字段 + 报告月时间窗**判断（§2.8.5：完成额当月及之后、开票/回款仅之后月份）；`auto_calc` / `system_sync` 及窗外观列不适用。
  - **实现：** Luckysheet `luckysheetCellBg`；经典 HTML 表 `.field-editable`（`cellClass` / `cellTdStyle`）。
- **🎨 单元格底色叠色优先级（已实现）：** 同一格仅展示一种主底色，优先级从高到低：

  | 优先级 | 场景 | 底色 / 样式 |
  |---|---|---|
  | 1 | 本月有变更 | 浅橙 `#fff7ed` + 深橙字 + 左边框 |
  | 2 | **存量预警（R/S 为负）** | 浅红底 `#fee2e2` + 红字 `#dc2626`（见 **§2.7.1**） |
  | 3 | 当前可编辑列 | 浅黄 `#fefce8` |
  | 4 | 本月新增项目（仅该行只读格） | 浅绿 `#d9e7d8` |
  | 5 | 只读 / 公式 / 斑马纹 | 灰白交替或 `#f8fafc` |

  即：**变更 > 存量预警 > 可编辑列 > 新增项目行**；R/S 列在值为负时优先展示预警色（覆盖可编辑/新增行底色）。
- **📝 变更单元格高亮**：
  - **当月变更**：当月值与上月快照不一致的字段（含新填报、修正、系统同步更新）。
  - **未来预测变更**：修改了未来月份的预测数据（Forecast），同样视为变更并高亮显示。
  - **视觉标识（已实现，Luckysheet / 图例 / HTML 表统一）：**

    | 元素 | 色值 / 说明 |
    |---|---|
    | 单元格背景 | `#fff7ed`（浅橙，`--color-changed-field-bg`） |
    | 文字颜色 | `#b45309`（深橙，`--color-changed-field-text`） |
    | 左边框 | `#f59e0b`，宽约 3px（Luckysheet 用 `bd`；HTML 用 `::before`） |

    填报页图例「本月有变更字段」展示与上表一致的迷你样块（浅橙底 + 橙字「Aa」+ 左边框），不再使用旧版「仅 4px 色条」示意。
  - **判定范围：** 高亮与批注以 `_changed_fields` **或** `_field_change_log` 中存在的列为准（`ChangeMeta.hasFieldChangeMarkup`），避免二次保存后仅保留最后一次编辑列导致历史变更丢失展示。
- **💬 变更单元格批注（已实现）：** 对有变更的格子自动附加 Luckysheet 批注（`ps`），悬停/点击角标可查看；**每条记录一行**，格式为 `【操作角色】修改前值 → 修改后值`（示例：`【PM】0 → 100`），**不展示**字段名与修改时间；**不同角色**对同一格的多次变更按时间顺序**多行堆叠**（如 `【PM】0 → 100` 下一行 `【板块总监】100 → 120`）。
  - **去重规则（已实现）：** 以 `角色 + 旧值 + 新值` 为键；`mergeChangeTracking` 合并时去重；`recordFieldChangeLog` 若与上一条完全相同则不再追加；`handleCellEdit` 仅以 Store 中项目为变更日志来源（避免表格行与库重复合并）；`flushLuckysheetToStore` 以 Store 当前值为 `oldVal` 基准，避免 `cellUpdated` 与「保存」重复记同一笔。
  - 在线编辑、`flushLuckysheetToStore`、填报页 Excel 合并导入、审批页总监/群主编辑均会写入 `_field_change_log`。
  - 公式重算（`recalcLuckysheetFormulas`）后按行重新同步批注与高亮，避免仅当前格保留 `ps`。
  - 经典 HTML 表（若启用）对变更格使用 `.field-changed` 类，并以 `title` 展示相同文案。
- **⚡ 联动计算高亮**：因手工修改导致**自动计算的字段**（如 WIP、累计回款、差值等）发生数值变化时，该计算字段单元格同步触发高亮，确保用户感知数据变动。
- **👁️ 视图过滤控制**：填报页 **表格工具栏**（`.sheet-toolbar`，**§2.12.1**）与**总监/群主审批页**（**§2.14**）均提供 `全部` / `新增` / `有变更` / **`预警`** 筛选，方便聚焦本月工作内容（预警见 **§2.7.1**）。
- **⏳ 保留期限：** 在**未生成**本轮填报基准快照 / 正式提交快照之前，同一项目内**历次**编辑产生的变更批注与 `_field_change_log` 均应保留（合并写入，不随后续保存覆盖）；PM 提交快照、板块 Draft 等版本节点另按 **§2.6**、**§2.11** 管理。
- **⚙️ 技术实现：** 基于月度快照表进行行级与字段级 `Diff` 计算；前端样式见 `css/style.css` 与 `js/change-meta.js`（`CHANGED_FIELD_STYLE`、`EDITABLE_FIELD_STYLE` / `applyLuckysheetChangedStyle`）；存量预警见 `js/stock-validation.js`；填报页 `ProjectEditor.js` 中 `luckysheetCellBg`、`isEditableDataCell`。

#### 2.7.1 存量指标预警（R / S 列，已实现）

字段字典分区 **「存量指标」**（列 **R–S**）：

| 列 | 字段名 | 公式 | 业务含义 |
|---|---|---|---|
| **R** | **存量开票额** | `= P − AC` | 总合同额减始累开票。正数表示尚未开票的合同部分；**小于 0** 表示累计开票已超过总合同额 → **预警**（允许存在，不阻断提交）。 |
| **S** | **存量合同额** | `= P − U` | 总合同额减始累完成。表示尚未完成的合同部分；**不得为负**；约束报告月及之前各月**月度完成额**（AV–BG）的累计结果。 |

- **预警样式（已实现）：** R 或 S **< 0** 时，该格使用红字 `#dc2626`、红底 `#fee2e2`（`--color-stock-warning-*` / `.field-stock-warning`）；Luckysheet 与经典 HTML 表一致。
- **预警筛选（已实现）：** 表格工具栏「**预警（N）**」仅展示 R 或 S 为负的项目（`StockValidation.hasStockWarning`）。
- **完成额编辑校验（已实现）：** 编辑 **AV–BG 月度完成额** 时，若变更会使 **存量合同额（S）变得更负**，则 **禁止保存**（Luckysheet `cellUpdateBefore` + `handleCellEdit`）；**允许调减**完成额以修复 CRB 合同下调后已为负的存量。
  - **提示文案：** 「累计完成额将超过总合同额（存量合同额将为负）。请调减完成额，或先在 CRB 办理合同额变更后再填报。」
- **提交阻断（已实现）：** PM「提交」、板块管理员「提交审批」前校验范围内全部项目 **S ≥ 0**；否则按钮禁用（文案「存量合同额为负，请调减完成额」）并弹出汇总错误。典型场景：平台同步后 **P 下调**导致填报前 S 已为负，须先调减各月完成额直至 S 恢复非负。
- **与平台同步的关系（§2.2）：** CRB 更新 **P** 或财务更新开票后，R/S 会随公式重算变化；系统同步**不**写入 `_changed_fields` 高亮，但预警色与筛选即时反映。
- **实现：** `js/stock-validation.js`；`fields-data.js` 列 R/S 中文名与说明；`ProjectEditor.js` 筛选 / 样式 / 提交校验。

---

### 2.8 Luckysheet 表格布局与列对齐（填报页）

与源 Excel（S520，83 列字段字典）保持一致，避免「代码里的 E 列」与「界面上看到的 E 列」不一致。变更高亮、**表格工具栏**项目筛选与紧凑列等交互见 **§2.7**、**§2.8.6**、**§2.12.1**；经典 HTML 表逻辑与 Luckysheet 对齐，但默认不向用户展示（见 **§2.1**）。

#### 2.8.1 列号对齐原则（已确认）

| 约定 | 说明 |
|---|---|
| **无额外序号列** | 不在数据区左侧增加「# / 序号」列；Luckysheet 行号由组件自带行头展示即可。 |
| **列字母 = 字段字典** | 字段字典中的列号（A、B、E、F…）与 Luckysheet 表头列字母、公式引用列字母**一一对应**（A→索引 0，E→索引 4）。 |
| **经典 HTML 表同步** | 经典表格视图同样不展示序号列，列顺序与字段字典一致。 |
| **公式引用** | Luckysheet 内 `auto_calc`、小计/合计行公式均按字段字典列字母书写（如 `E5` 表示字段 E、第 5 行），不再对全表做 +1 偏移。 |

#### 2.8.2 行布局（0-based，Luckysheet）

| 行号 | 用途 |
|---|---|
| 0 | **小计**（Subtotal）：金额列使用 `SUBTOTAL(9, 数据区)` |
| 1 | **合计**（Total）：金额列使用 `SUM(数据区)`；标签文案显示在 **G 列**（「小计 Subtotal」「合计 Total」） |
| 2 | 大类 / 分区标题行（按 `section` 合并单元格） |
| 3 | 字段中文标题行 |
| 4+ | 项目数据行（顺序与筛选结果一致） |

小计/合计置于大分类之上，**默认筛选**仅覆盖「字段标题行 + 数据行」，避免汇总行被筛掉。

- **横向滚动与冻结列：** 筛选按钮挂在可滚动的 `#luckysheet-cell-main` 内。横向滚动时对 **A–F 冻结列** 的按钮按 `left = 基准left + scrollLeft` 补偿，使其与冻结表头对齐；对滚入冻结区下方的可滚动列按钮则 `display:none` 隐藏。

#### 2.8.3 冻结与表头

- **冻结窗格：** 冻结至字段标题行（含小计、合计、大分类行）及 **F 列（项目号）** 左侧，横向滚动时保留项目识别关键列（A–F）。
- **表头展示：** 仅显示字段**中文名**，不在表头第二行重复展示列字母（如 `(E)`）。
- **可编辑列（表头 + 数据格）：** 当前角色、锁定状态下**可编辑**的列：
  - **表头：** 浅黄底 `#fef9c3` + 深黄字（`--color-editable-header-*` / `.editor-th-editable`）；
  - **数据格：** 整列浅黄底 `#fefce8`（`--color-editable-cell-bg` / `.field-editable`），与变更浅橙、新增行浅绿按 **§2.7** 叠色优先级处理。

#### 2.8.4 列宽与自定义宽度

- **列宽配置：** 支持按**字段字典列字母**配置窄列（如 E/F/G）；写入 Luckysheet `config.columnlen` 时键为**列索引**（与列字母一致映射）。
- **防止被自动撑开：** 对已配置 `columnlen` 的列，须同步设置 `config.customWidth` 为**按列索引的对象**（如 `{ "4": 1 }`），避免网格刷新后按单元格内容重算列宽导致配置失效。
- **默认列宽：** 未单独配置的列按字段 `colWidth` 或合理默认宽度（约 72–220px）处理。

#### 2.8.5 填报期内的列级只读（动态时间窗）

以系统**报告月**（如 `2026-05`，即 5 月）为基准，`FieldConfig.isMonthlyFieldEditable` / `canEdit` 对**所有角色（含系统管理员）**统一适用：

| 列组 | 列字母 | 可编辑月份（示例：报告月 5 月） | 数据性质 |
|---|---|---|---|
| **月度完成额** | AV–BG（1–12 月） | **5 月及之后**（`m >= 4`） | 当月为 PM 手工填报的实际/计划完成额；之前月份为历史只读 |
| **月度开票** | BH, BJ, … CD | **6 月及之后**（`m > 4`） | **当月**由财务系统同步**实际开票**，只读；之后月份为**预测值**可编辑 |
| **月度回款** | BI, BK, … CE | **6 月及之后**（`m > 4`） | **当月**由财务系统同步**实际回款**，只读；之后月份为**预测值**可编辑 |

- 非月度手工列（实施进展 M、WIP 分析 AM–AU 等）不受上表月份窗限制；系统管理员在锁定期仍可编辑此类列。
- Luckysheet 通过工作表保护 + 单元格 `lo` 标记 + `cellUpdateBefore` 二次校验，与 `canEditField` / `isPastReportingMonthField`（= 月度窗不可编辑）规则一致。
- 经典 HTML 表对同期列应用相同只读样式（`month-locked-cell`）。
- Excel 导入合并（§2.12.3）同样跳过不可编辑月份格，避免覆盖当月开票/回款实际值。

#### 2.8.6 表格工具栏、Luckysheet 配置与右键

- **Luckysheet 内置顶栏（已实现）：** `showtoolbar: false`，**不提供**撤销/还原、查找、筛选等 Luckysheet 原生工具按钮；`showinfobar: false`、`showsheetbar: false`、`showstatisticBar: false`；`sheetFormulaBar: false`（隐藏公式栏）；`lang: 'zh'`（列头筛选下拉等 Luckysheet 原生 UI 为简体中文）。
- **自定义表格工具栏 `.sheet-toolbar`（已实现）：** 紧贴 Luckysheet 上方（原 Luckysheet 工具栏位置），高度约 **30px**，填报页与 `ApprovalReviewSheet` 共用样式；**左 → 右**：
  1. **项目筛选**（`el-radio-group`，紧凑 **26px**）：**全部** / **新增** / **有变更** / **预警**（审批页无「预警」）；切换后 `buildTableData()` + 刷新 Luckysheet。
  2. **紧凑列显示**（`el-checkbox`）：勾选「**仅显示项目信息与可编辑列**」→ 写入 `config.colhidden` 隐藏其余列（**列索引不变**，不影响 `cellUpdated` 入库与列字母映射）。可见列 = `section === 项目基本信息` **∪** 当前角色 `FieldConfig.canEdit`（`canEditField`）；大分类行合并范围与分区标题格随可见列调整（`buildLuckysheetMerge` / `buildLuckysheetCelldata`）。
- **业务工具栏 `.editor-toolbar`：** 版本、周期横幅、导出/导入/同步、保存/提交等，见 **§2.12.1**；与表格工具栏分离，避免单行过挤。
- **单元格右键：** 仅保留**列筛选**（`filter: true`）；行/列标右键关闭插删行列与改宽高。
- **下拉枚举：** 对含 `enum_values` 且当前可编辑的单元格，下发 Luckysheet 数据验证（dropdown）；选项文本内含英文逗号时跳过该校验格，避免解析错误。
- **金额列（`data_type = 金额`，已实现）：**
  - **单元格类型：** `ct: { t: 'n', fa: '#,##0.00' }`；`v` 为数值型。
  - **水平对齐：** `ht: '2'`（右对齐），含数据行、公式格、小计/合计行及列标题行。
  - **可编辑格校验：** `dataVerification` 类型 `number` + `prohibitInput`；`cellUpdateBefore` 拦截非数字输入；入库经 `coerceFieldValue` 去千分位后转 `Number`。
  - **实现：** `field-config.js`（`luckysheetCt` / `luckysheetHt`）、`ProjectEditor.js`（`applyLuckysheetAmountColumnStyle`、`buildLuckysheetDataVerification`）；审批页 `ApprovalReviewSheet` 继承同一套逻辑。
- **公式链：** 创建 sheet 时预构建二维 `data` 矩阵并配置 `calcChain`（sheet `index` 为数字 `0`；先数据行公式，再小计/合计行），避免初始化时 `mergeCalculation` 访问空行报错；加载后触发公式重算，保证 SUBTOTAL/SUM 与 `auto_calc` 正确显示。
- **布局自适应：** 侧栏折叠/展开时通过 `ResizeObserver` 与 `resize` 事件重算 Luckysheet 画布宽度，使表格区域随主内容区变宽/变窄。
- **默认筛选：** 表创建后对「字段标题行 + 数据行」范围开启筛选（`filter_select` + `setRangeFilter('open')`），小计/合计/大分类行在筛选区外。
- **存量合同额校验（已实现，详见 §2.7.1）：** 月度完成额（AV–BG）编辑时校验 S 列；PM / 板块提交前校验范围内项目 S ≥ 0。

#### 2.8.7 提交审批后的填报锁定

- **触发（板块级）：** 板块管理员对**本板块**点击「提交审批」并成功生成 **`Draft:<板块编码>`** 快照后，系统设置该板块在 `meta.sectorFlows[code].reportingSubmitted = true`（持久化于 SQLite `meta`）；各板块互不影响。
- **填报页：** 本板块「提交审批」按钮禁用并显示「已提交审批」；Luckysheet 与经典 HTML 表（若启用）在**非系统管理员**角色下，当**本板块**已提交时进入只读，与锁定期规则叠加。
- **解除：** 该板块审批驳回、管理员在管理页「从初始 Excel 恢复」或开发环境「重置为初始状态」时清除该板块标记；系统管理员在已提交状态下仍可编辑。

---

### 2.11 项目经理提交与板块接收

项目经理的「提交」是面向板块管理员的内部汇报动作，不等同于进入审批流的 Draft 提交。

#### 角色页面权限

| 角色 | 可见页面 |
|---|---|
| **项目经理** | 数据看板、填报表格（隐藏审批流程、审计日志、管理设置） |
| **板块总监** | 数据看板、审批流程（隐藏填报表格、审计日志、管理设置） |
| **项目群群主** | 数据看板、审批流程（同板块总监） |
| **板块管理员** | 全部页面 |
| **财务审核** | 数据看板、填报表格、审批流程、审计日志（无管理设置） |
| **系统管理员** | 全部页面 |

- 项目经理登录后默认进入数据看板；看板和填报表格的数据均按 **`pm_name === 当前用户`** 过滤，仅展示本人负责项目。
- 板块总监 / 项目群群主直接访问 `/editor`、`/audit`、`/admin` 时重定向至数据看板（`js/router.js`）。
- 模拟项目经理账号：**何孝刚**（5 条项目）、**宋建生**（2 条项目），与数据库 `pm_name` 字段一致。

#### 提交状态机

```
[可编辑] ──提交──> [submitted: 待板块接收，PM 锁定]
                         │
              板块管理员确认接收
                         │
[可编辑] <──received────┘
（板块正式提交审批前可重复上述循环）
                         │
              板块管理员提交审批（生成 Draft）
                         │
                  [板块锁定，PM 不可再提交]
```

#### 数据存储

- `meta.pmSubmissions`：按报告月分桶，记录每个 PM 的提交状态（`submitted` / `received`）、提交时间、快照版本及项目号列表。关键字段：
  - `snapshotVersion`：本次提交快照（`PM:<姓名>:<月>:<时间戳>`）。
  - `baselineSnapshotVersion`：当前填报轮次的**编辑基准**（下一轮继续用）。
  - `submissionBaselineSnapshotVersion`：本轮提交时冻结的基准，专供「查看变更」对比，**确认接收时不覆盖**。
- **填报基准快照**（版本名含 `:baseline:`）：PM 进入可编辑状态后、开始改数之前的项目子集；用于 diff，不等同于提交快照。
- PM 子集快照（`snapshots` 表）：仅含该 PM 当时负责的项目；板块管理员可通过 `GET /api/snapshots/:version` 拉取。
- `resetDevMeta` / `reseed` / 开发重置时同步清空 `pmSubmissions` 与 PM 子集快照。

#### 填报基准的建立时机

| 时机 | 行为 |
|---|---|
| PM 首次打开填报页（`status` 非 `submitted`） | 前端将当前表格中该 PM 项目**克隆**后调用 `POST /api/pm-submissions/ensure-baseline`，避免异步建基准时库中已写入未落库的编辑导致「基准≈提交、diff 为空」。 |
| 板块管理员「确认接收」后 | 服务端按**接收瞬间库数据**新建下一轮 `baselineSnapshotVersion`；`submissionBaselineSnapshotVersion` 保留供历史 diff。 |
| PM 已 `submitted` 待接收 | 不再新建基准，避免覆盖本轮对比起点。 |

#### PM 编辑保存与提交（Luckysheet）

- **单元格保存：** 可编辑格变更经 `cellUpdated` → `PUT /api/projects/:projectNo` 写入 SQLite，并合并更新 `_changed_fields`、`_field_change_log`（见 **§2.5**、**§2.7**）及审计日志。
- **手动保存：** 工具栏「保存」与提交前兜底均调用 `flushLuckysheetToStore()`（见 **§2.12.2**）；保存结束后全表同步 Luckysheet 变更批注与高亮。
- **Excel 导入：** PM 可在填报页「上传导入」，仅合并本人项目可编辑列（见 **§2.12.3**）；导入产生的变更同样写入 `_field_change_log`。
- **提交前兜底（已实现）：**
  1. 调用 Luckysheet `exitEditMode`，结束未失焦的编辑态；
  2. 扫描 sheet 中所有可编辑的手工列，将未触发 `cellUpdated` 的值补写入库；
  3. **存量合同额校验（§2.7.1）：** 范围内项目 **S ≥ 0**，否则中止提交并提示；
  4. `Store.syncPmProjectsToServer` 将该 PM 名下项目全量同步至服务端；
  5. 再调用 `POST /api/pm-submissions/submit`（**仅从库中读取**该 PM 项目生成快照）。
- **提交后界面：** 用服务端返回的提交快照回写本地 `Store.projects` 中对应项目，再刷新 Luckysheet，保证展示与已提交内容一致。
- **注意：** 灰色/公式列（`auto_calc`、`system_sync`）不可保存；可入库的手工列含实施进展 M、WIP 分析、**报告月及之后**的月度完成额、**报告月之后**的月度开票/回款预测、**预测开票时间 AU** 等（见 **§2.8.5**）。

#### 板块管理员操作

- 填报页工具栏下方显示**待接收面板**，列出本月 `status === 'submitted'` 的 PM（姓名、项目数、提交时间）。
- **「查看变更」**（字段级 diff，仅对比 `manual_input` 列）按以下优先级：
  1. **填报基准 | 提交值**：`submissionBaselineSnapshotVersion`（或 `baselineSnapshotVersion`） vs 提交快照；
  2. 若无差异则 **提交快照 | 当前追踪表**：该 PM 在库中的最新数据（含接收后管理员改动）；
  3. 仍无则展示提交快照中的 **`_changed_fields`** 列清单。
- 弹窗列标题随对比模式变化；若无 diff 会提示可能原因（改了公式列、他人项目、基准异常等）。
- **「确认接收」**：`status` → `received`，PM 解锁；新建下一轮 `baselineSnapshotVersion`；写审计日志。
- 板块管理员汇总确认后，点击「提交审批」生成该板块 `Draft:<板块编码>`，该板块 `reportingSubmitted = true`，本板块 PM 不可再提交（其他板块不受影响）。

#### 与「管理员全表」的差异说明

- PM 提交快照仅含 **`pm_name` 匹配** 的项目；板块管理员填报页为**全板块**项目。
- 「查看变更」对比的是 **PM 本轮改动**，不是「提交版 vs 管理员整张表」；全表差异可能来自其他 PM、管理员改数或自动重算列。

#### 相关 API（当前实现）

| 接口 | 说明 |
|---|---|
| `POST /api/pm-submissions/ensure-baseline` | 建立填报基准；body 可带 `projects` 覆盖服务端读取 |
| `POST /api/pm-submissions/submit` | PM 提交，写快照与 `pmSubmissions` |
| `POST /api/pm-submissions/receive` | 板块接收，解锁 PM |
| `GET /api/snapshots/:version` | 按版本取快照（含 PM 子集） |

---

### 2.8.8 日期字段与 Excel 序列号（Luckysheet）

部分日期列（如 **AU 预测开票时间**、`data_type = 日期`）在 Luckysheet/Excel 内部常以 **序列号**（如 `45748`）存储。若直接入库，提交刷新后会显示为数字而非 `YYYY-MM-DD`。

**统一规则（已实现）：**

| 环节 | 行为 |
|---|---|
| **保存 / 导入** | `Formatters.normalizeDateValue` 将序列号、`Date`、文本统一为 **`YYYY-MM-DD`** 字符串再写入库 |
| **Luckysheet 展示** | `ct: { fa: 'yyyy-MM-dd', t: 'd' }`，`m` 为 ISO 日期，`v` 为对应 Excel 序列日 |
| **Excel 导入** | `xlsx-importer.js` / `server/xlsx-seed.js` 对 `data_type === '日期'` 列做同样规范化 |

**只读日期列**（如 CRB 同步的约定开始/结束时间 B、C）展示时同样走上述格式化，避免历史数据中的序列号裸显。

---

### 2.9 主布局：侧边栏折叠

- **折叠/展开入口（已实现）：** 折叠按钮位于**侧栏底部**（非 Logo 区、非顶栏）。
  - **展开态：** 底部展示「填报月份 + 填报期状态灯」，其下为「收起菜单」按钮（`el-icon-s-fold`）。
  - **折叠态：** 底部**仅显示「展开」按钮**（`el-icon-s-unfold`），**不再显示**填报期圆点状态灯；Logo 区缩为「金」字图标。
- **导航：** 折叠后菜单仅图标，悬停显示标题（Element UI `el-menu :collapse`）。
- **宽度：** 展开约 220px、折叠约 64px，带过渡动画；主内容区 `flex:1` 随侧栏变宽。
- **状态记忆：** `sidebarCollapsed` 写入浏览器 `localStorage`（`ptrack_sidebar_collapsed`），刷新后保持。

### 2.12 填报页工具栏、导入与历史版本（已实现）

填报表格页（`ProjectEditor.js`）自上而下：**业务工具栏** → **图例条**（可关闭）→ **表格工具栏** → **Luckysheet** 画布。

#### 2.12.1 工具栏布局

##### 业务工具栏（`.editor-toolbar`，紧凑）

| 区域 | 内容 |
|---|---|
| **左侧** | 「版本」下拉 → 填报期状态 **或** 快照提示条（二选一，见下） |
| **右侧** | **导出 Excel · 上传导入 · 更新系统数据**（系统管理员，一组）｜ **保存 · 提交归档 · 提交/提交审批**（一组，竖线分隔） |

- **版本下拉**在填报期状态标签**左侧**；占位符为「版本」。
- **查看快照时：** 隐藏「填报窗口开放中」等 `period-banner`，改为 `editor-snapshot-badge`：**「当前正在查看快照 · {时间}」**（审批版快照可带阶段前缀，如「草稿 · 时间」）；**不显示**操作人姓名。
- **版本选项文案：** 下拉列表与选中展示均使用**格式化时间**（含秒），不展示 `PM:姓名:…:时间戳` 等内部版本 ID；时间优先取快照 `time`，否则解析版本名末尾时间戳。
- **项目筛选与紧凑列**已移至下方 **表格工具栏**，本栏不再放置「全部 / 新增 / 有变更 / 预警」。

##### 表格工具栏（`.sheet-toolbar`，Luckysheet 正上方）

| 区域 | 内容 |
|---|---|
| **左侧** | 项目筛选：**全部（N）** / **新增（N）** / **有变更（N）** / **预警（N）**（与 **§2.7**、**§2.7.1** 联动） |
| **右侧** | 勾选「**仅显示项目信息与可编辑列**」（`compactColumnsOnly` → `buildLuckysheetColhidden()`） |

- **实现：** `ProjectEditor.js`；审批页 `ApprovalReviewSheet.js` 继承同一套逻辑（筛选无「预警」项；右侧可保留板块提示文案 `reviewHintText`）。
- **样式：** `css/style.css` 中 `.sheet-toolbar`、`.view-toggle--compact`（筛选按钮高约 **26px**）；业务栏 `.editor-toolbar` 内边距约 **6×12px**。

#### 2.12.2 保存

- **入口：** 「保存」按钮（`canEdit` 时显示）。
- **行为：** 调用 `flushLuckysheetToStore()`：结束 Luckysheet 编辑态、等待进行中的单元格保存、扫描可编辑列补写未触发 `cellUpdated` 的格子；**不**生成审批快照、**不**触发 PM 提交流程；完成后 `buildTableData()` 并 `syncAllLuckysheetChangeDecor()`，恢复全部变更格的批注与高亮。
- **提交归档（系统管理员，已实现）：** 与「保存」同组，按钮文案「**提交归档**」；仅 `role === system_admin` 且非快照预览、公司尚未归档（`companyFlow.archiveStatus !== final`）时显示；调用 `Store.archiveCompany()` → `POST /api/company/archive` 生成 **J版**；**不**要求全部板块 Approve2。
- **说明：** 平时单元格修改已逐格 `PUT` 入库；「保存」用于刷盘未失焦的编辑。每次 `handleCellEdit` 从 Store 合并历史 `_field_change_log` 并去重后追加本条，避免「保存 → 再改 → 再保存」后先前批注丢失或与 `cellUpdated` 重复记同一变更。

#### 2.12.3 上传导入（填报页）

- **入口：** 「上传导入」（`canImport`：系统管理员 / 板块管理员 / 项目经理；财务与审批角色不可导入；`canEdit` 且非 PM 锁定、板块未提交审批）。
- **解析：** `XlsxImporter.importFromFile`（与 Excel 列顺序、字段字典一致）。
- **合并规则（`js/import-merge.js`）：**
  - 以 **`project_no` 为唯一键**；
  - 仅当 `FieldConfig.canEdit` 为真时覆盖对应字段；`system_sync` / `auto_calc` 及不可编辑列**忽略**文件中的值；
  - 库中无匹配项目号的行**跳过**（不新增项目）；
  - 合并后 `FormulaEngine.compute` 再 `PUT` 逐条更新；
  - **PM：** 仅处理 `pm_name === 当前 PM` 的导入行；
  - 合并时通过 `ChangeMeta.mergeChangeTracking` 保留既有 `_field_change_log`，并为每个被覆盖字段追加批注元数据；
  - 写入审计日志（`fieldCN: 填报页导入`）。
- **与管理页区别：** 管理设置「导入」仍为 `replaceProjects` **全量覆盖**。

#### 2.12.4 历史版本（表格区域内查看）

- **入口：** 版本下拉：「当前填报」+ 快照列表。
- **列表范围：**
  - 固定顺序优先：`Draft` → `Approve1` → `Approve2` → `J版`；
  - 其余（含 `PM:<姓名>:…`）按快照时间降序；
  - **PM** 仅见本人 `PM:` 前缀快照 + 若存在的全局审批版（只读查看）。
- **切换行为：** 非「当前填报」时 `fetchSnapshot` → Luckysheet **只读**展示该版本项目（经角色范围过滤）；`canEdit` 为 false。
- **提交按钮：** 查看快照时**隐藏**「提交」/「提交审批」（不仅禁用）；「保存」「上传导入」同步隐藏；右侧整组操作区收起。
- **与审批页：** 板块管理员等角色在审批页可查看版本快照与双版本 Diff；总监/群主在审批页使用独立 Luckysheet（**§2.14**，非版本快照 Diff）。填报页提供**就地预览**，不替代审批能力。

#### 2.12.5 导出与提交

- **导出 Excel：** 导出当前 `Store.projects`（全库），与版本下拉所选无关。
- **提交：** 逻辑不变（PM「提交」/ 板块「提交审批」）；见 **§2.11**、**§2.8.7**。

#### 2.12.6 图例说明条

- **位置：** 业务工具栏下方、**表格工具栏上方**；可关闭（`showDiffHint`）。
- **项与样式：**

| 图例文案 | 含义 | 示意 |
|---|---|---|
| 本月新增项目 | `_added_this_month`（该行只读格） | 浅绿底色块 `#d9e7d8` |
| 可编辑列 | 当前角色可填报列（数据格） | 浅黄底色块 `#fefce8`（表头同色加深） |
| **本月有变更字段** | `_changed_fields` / `_field_change_log` | 与 Luckysheet 变更格一致（浅橙底 + 橙字 + 左边框） |
| 系统只读字段 | `system_sync` / `auto_calc` 等 | 灰底色块 |

- **样式类：** `editor-diff-hint`、`editor-legend-swatch--*`（见 `style.css`）。

#### 2.12.7 变更批注模块（`js/change-meta.js`）

| 能力 | 说明 |
|---|---|
| `recordFieldChangeLog` | 向 `_field_change_log[col]` 追加一条；与上一条「角色+旧+新」相同则跳过 |
| `dedupeChangeLogList` / `changeLogEntryKey` | 按「角色+旧值+新值」去重，保留首次出现顺序 |
| `mergeChangeTracking` | 合并多来源 `_field_change_log` 与 `_changed_fields`（合并时去重） |
| `formatChangeComment` | 多行 `【角色】旧值 → 新值`（`getFieldChangeEntries` 返回去重后列表） |
| `attachChangeTracking` | 公式重算后的 `tableProjects` 从 `Store` 回挂元数据 |
| `resolveFieldChangeMeta` | 取该列最近一条变更（审计回退等） |
| `applyLuckysheetChangedStyle` | Luckysheet 变更格高亮（`CHANGED_FIELD_STYLE`） |
| `EDITABLE_FIELD_STYLE` | 可编辑列数据格底色常量（与 `--color-editable-cell-bg` 一致） |
| `syncLuckysheetProjectRowDecor` / `syncAllLuckysheetChangeDecor` | 公式重算或保存后按行/全表恢复 `ps` 与高亮 |

### 2.13 上月归档快照与新增项目演示（已实现）

| 项 | 说明 |
|---|---|
| **版本键** | `Month:2026-04`（由当前 `reportingMonth` 自动推导上一月） |
| **快照内容** | 当前库项目的子集（**默认剔除 5 条** `project_no`），按**上月** `monthIdx` 重算 `FormulaEngine`，并清零上月之后的月度完成/开票/回款 |
| **meta** | `priorMonthSnapshotVersion` 指向当前使用的对比快照键 |
| **前端** | `js/project-month-diff.js` 在 `buildTableData` 中为不在快照中的项目设 `_added_this_month` |
| **开发重置/重导** | `server/dev-reset-seed.js` 在 **§2.10** 重置、`reseed`、首次空库导入后自动生成 `Month:上一月` 快照，并固定 **5 条**演示「本月新增」项目（`meta.demoNewProjectNos`） |
| **注意** | 单独「从初始 Excel 恢复」会清空全部快照；开发重置与 `reseed` 会自动重建上月对比快照 |

### 2.14 审批决策角色界面（板块总监 / 项目群群主，已实现）

> 实现：`js/views/Approval.js`、`js/components/ApprovalReviewSheet.js`（`extends ProjectEditorView`，与 `js/views/ProjectEditor.js` 同源 Luckysheet 能力）、`js/components/AppLayout.js`、`js/router.js`、`js/field-config.js`（总监/群主可写列）。

#### 导航与顶栏

| 项 | 说明 |
|---|---|
| **侧栏** | 仅 **数据看板**、**审批流程**（路由 `/dashboard`、`/approval`） |
| **顶栏** | 仅页面标题 + 用户菜单；**不展示**「审批：草稿 / 初审中」等审批状态 Tag |
| **内容区** | 审批页 `no-padding`，右侧表格区纵向占满（`css/style.css` `.approval-reviewer-*`） |

#### 页面布局（左右分栏）

| 区域 | 宽度 | 内容 |
|---|---|---|
| **左侧** | 300px（与 `.approval-layout` 一致） | 卡片标题 **流程进度**（非「审批流程」）+ 三态图例 + 四节点时间轴 + 通过/驳回 |
| **右侧** | 剩余宽度 | Luckysheet（`#approval-luckysheet-mount`），**无** Luckysheet 内置顶栏、**无**填报页业务工具栏；Luckysheet 上方为 **表格工具栏**（项目筛选 + 紧凑列勾选，见 **§2.12.1**） |

#### 流程进度时间轴（三态）

| 状态 | 圆点样式 | 节点标签 | 含义 |
|---|---|---|---|
| **已完成** | 品牌绿 + 勾 | `el-tag` success | 该步骤已结束 |
| **进行中** | 橙色 + 「···」 | `el-tag` warning | 当前待处理步骤 |
| **未完成** | 灰色 + 步骤序号 | `el-tag` info | 尚未到达 |

- 卡片顶部图例与上表一致（已完成 / 进行中 / 未完成）。
- **进行中节点计算（`activeFlowIdx`）：** `meta.approvalStatus` 表示**上一节点已完成**后的状态，而非「正在进行的节点」。示例：
  - `draft` 且未 `reportingSubmitted` → 节点 0（提交填报）进行中；
  - `draft` 且已提交 → 节点 0 已完成、节点 1（板块总监初审）进行中；
  - `approve1` → 节点 0–1 已完成、节点 2（群主复审）进行中；
  - `approve2` → 节点 0–2 已完成、节点 3（归档）进行中；
  - `final` → 全部为已完成。

#### 右侧 Luckysheet（本板块当月全量 + 筛选）

- **数据范围：** 本板块（`unit_code`，默认 `S520`）**当月全部项目**（与板块管理员填报范围一致），默认展示 **全部**；**表格工具栏**单选：**全部** / **新增**（`_added_this_month`）/ **有变更**（`_changed_fields` 或 `_field_change_log` 非空）；筛选结果为空时显示空状态文案。
- **交互：**
  - **非待办节点**或全局锁定：只读（`canEdit = false`）。
  - **待办节点内可编辑：** 板块总监在 `draft` 且 `reportingSubmitted` 时；项目群群主在 `approve1` 时。可编辑列与板块管理员相同（`SECTOR_ADMIN_EDITABLE_COLS`，受 `lockStatus` / 历史月锁定约束），修改经 `handleCellEdit` 入库并追加 **§2.7** 批注。
- **视觉：** 保留 **§2.7** 新增行底色、可编辑列浅黄、变更格高亮与批注图例（可关闭）。
- **实现要点：** `ApprovalReviewSheet` 继承 `ProjectEditorView`，覆盖 `scopedProjects` / `filteredProjects` / `canEdit` / `reviewerEditActive`；`showtoolbar: false`、`lang: 'zh'`、独立 `lsGridKey`（`ptrack_approval_review`）、挂载点 `#approval-luckysheet-mount`；与填报页共用 `.sheet-toolbar` 与 `compactColumnsOnly` / `buildLuckysheetColhidden()`；脚本须在 `ProjectEditor.js` 之后加载（IIFE 传入 `window`）。
- **变更批注：** 待办节点内编辑与填报页共用 `handleCellEdit` + **§2.7** 批注规则（含去重）；无填报页「保存/导入/提交」工具栏。

#### 通过 / 驳回

| 操作 | 行为 |
|---|---|
| **通过** | 与 **§2.6** 一致：推进 `approvalStatus` 并生成 `Approve1` / `Approve2` / `J版` 快照；仅当前待办角色可见按钮（`canApprove`） |
| **驳回** | 与通过同一待办权限（`canReject === canApprove`）；回退 `draft` + `reportingSubmitted: false`；提示「退回板块管理员」；可选填写驳回原因写入 `audit_log` |

#### 其他角色的审批页（对照）

- **板块管理员 / 财务等：** 左侧 **流程进度** 时间轴（三态 + 图例）；右侧 **版本快照记录** + **版本差异对比**，**不**嵌入 `ApprovalReviewSheet`。
- **系统管理员：** 使用独立 **§2.15** 审批页（十二板块并行流程卡片），**无**左侧四节点时间轴、**无**公司归档区、**无**版本快照列表；**J版** 归档仅在 **填报表格** 工具栏「提交归档」。

### 2.15 系统管理员：十二板块、审批页与填报底栏（已实现）

> **需求背景：** 公司各个执行单位（板块/中心）的审批流程**并行**、互不影响；系统管理员需在审批页纵览各板块三步进度，在填报页纵览状态并按板块查看变更，随时可提交 **J版** 全公司归档。

#### 十二板块注册表（`sectorRegistry` / `sectorNames`）
- **展示：** 卡片/底栏使用 `sectorDisplayLabel`，如 `SAS520 · 金山中心`。
- **归一：** 项目 `unit_code` 为 `S520` 等短码时，读写流程统一为 `SAS520`（`SectorWorkflow.normalizeSectorCode`）。
- **默认列表：** `DEFAULT_SECTOR_REGISTRY` 固定上述 12 项；`bootstrap` 时 `migrateSectorFlows` 为每板块初始化 `meta.sectorFlows[code]`。

#### 分板块审批与公司 J 版归档

| 层级 | 说明 |
|---|---|
| **分板块流程** | `meta.sectorFlows[code]`：`approvalStatus`（`draft` / `approve1` / `approve2`）、`reportingSubmitted`；十二板块独立。 |
| **分板块快照** | `Draft:SAS520`、`Approve1:SAS520`、`Approve2:SAS520` 等，仅含该板块项目。 |
| **公司 J 版** | 全局 **`J版`**，含**整库全部项目**；系统管理员**随时可提交归档**（已实现），**不**以「全部板块 Approve2」为前置条件。 |
| **迁移** | 旧版全局 `Draft` 等在 bootstrap 时迁移为 `Draft:SAS520`（`server/sector-workflow.js`）。 |

#### 审批流程页（`SystemAdminApprovalBoard`）

- **路由：** `/approval`；组件 `js/components/SystemAdminApprovalBoard.js`。
- **布局：** 全宽 **卡片网格**，每卡对应一板块，展示三步：**提交填报 → 总监初审 → 群主复审**（圆点三态：已完成 / 进行中 / 未完成）及 `sectorFlowStatusText` Tag。
- **明确不做（已实现）：** 无「公司归档」汇总区；无「提交归档」按钮；无右侧「版本快照记录」；无版本差异对比。
- **与总监/群主页：** 后者为左侧时间轴 + 右侧 `ApprovalReviewSheet`（**§2.14**）；系统管理员为另一套 UI，互不混用。

#### 填报页各板块进度底栏（`SystemAdminSectorDock`）

#### 与板块管理员「待接收 PM」底栏对照

| 维度 | 板块管理员（§2.11） | 系统管理员（本节） |
|---|---|---|
| **展示维度** | 按 **PM** 卡片 | 按 **板块**（12 个 SAS 编码）卡片 |
| **可见范围** | 仅 `status === submitted` 的 PM | **全部 12 板块**，无论流程是否结束 |
| **确认接收** | 有「确认接收」 | **无**（只读监控） |
| **入口** | 填报表格 `/editor` | 填报表格 `/editor` |
| **视觉样式** | 黄底 `#fffbeb`、顶边 `#f59e0b` | 同左（`.system-admin-sector-dock*`） |

#### 底栏 UI 与交互

- **挂载：** 填报页表格区**下方**（`ProjectEditor.js` 注册 `SystemAdminSectorDock`）。
- **显示：** 系统管理员**始终显示**；主表为**全公司全量项目**（不按板块筛选主表）。
- **填报期横幅：** 系统管理员固定为「可编辑全部项目数据」开放态（不受板块 `reportingSubmitted` 锁定）。
- **标题区：** 「各板块进度（12 个）」；Tag：「公司已归档 J版」/「全部板块已完成审批」/「N 个板块审批中」；**收起 / 展开**折叠卡片列表。
- **板块卡片：** `SAS520 · 金山中心` 等 + 流程状态 Tag；**查看变更**（弹窗 Diff，对齐板块管理员 PM diff 样式）。
  - **查看变更：** 仅当该板块 **`approvalStatus === approve2`（已完成审批）** 时可点；否则按钮 **disabled**。
  - **对比逻辑：** 优先 `Draft:<code>` 快照 vs 当前库 `manual_input` 列；无快照则展示 `_field_change_log` 元数据。
- **明确不做：** 无「确认接收」；无底栏「公司归档」/「版本比对」；主表不因点击板块而筛选（已移除「仅新增/变更」主表筛选）。

#### 与审批页、板块提交的关系

- **板块管理员：** `POST /api/sectors/:code/submit-approval` → `Draft:<code>`（**§2.8.7**、**§2.11**）。
- **板块总监 / 群主：** **§2.14**；`advance-approval` / `reject-approval`。
- **系统管理员：** 审批页见上节；**J版** 仅在填报页工具栏「**提交归档**」；成功后底栏 Tag「公司已归档 J版」。

#### 相关实现

| 文件 | 说明 |
|---|---|
| `server/sector-workflow.js`、`server/db.js`、`server/index.js` | 十二板块注册、`sectorFlows`、`companyFlow`、分板块与公司归档 API |
| `js/sector-workflow.js`、`js/diff-utils.js` | `listSectors`、`sectorDisplayLabel`、`sectorFlowStatusText`、板块过滤 |
| `js/store.js` | `getSectorFlow`、`archiveCompany` 等 |
| `js/components/SystemAdminApprovalBoard.js` | 审批页十二板块流程网格 |
| `js/components/SystemAdminSectorDock.js` | 填报页底栏 |
| `js/views/ProjectEditor.js` | 工具栏「提交归档」、`canShowArchiveButton` |
| `css/style.css` | `.sysadmin-*`、`.system-admin-sector-dock*` |

#### 相关 API

| 接口 | 说明 |
|---|---|
| `POST /api/sectors/:code/submit-approval` | 板块管理员提交本板块审批 → `Draft:<code>` |
| `POST /api/sectors/:code/advance-approval` | 总监/群主通过 → `Approve1:<code>` / `Approve2:<code>` |
| `POST /api/sectors/:code/reject-approval` | 驳回本板块，回退 `draft` 并清除该板块 `reportingSubmitted` |
| `POST /api/company/archive` | 系统管理员全公司归档 → 全局 `J版`（**不**校验全部板块 Approve2） |

### 2.10 开发测试：环境重置

> 面向本地/联调环境，便于反复验证填报与审批流程；**上线前建议隐藏或仅开放给系统管理员**。

- **入口：** 顶栏右上角用户下拉菜单 → **「重置为初始状态（开发）」**（二次确认）。
- **接口：** `POST /api/admin/reset-dev`（需重启 `npm start` 后加载的最新服务端）。
- **重置范围：**

| 项 | 行为 |
|---|---|
| 项目数据 | 从项目根目录「初始数据.xlsx」（或 S520 源表 / `PTRACK_INIT_XLSX`）重新导入，覆盖当前库 |
| 审批流程 | `approvalStatus` → `draft`，`reportingSubmitted` → `false` |
| 快照与审计 | 清空后**自动**写入上一报告月对比快照 `Month:YYYY-MM`（如 `Month:2026-04`），并剔除 5 条固定演示项目作为「本月新增」 |
| 填报周期 | `periodConfig`、`reportingMonth` 恢复 `DEFAULT_PERIOD_CONFIG` 默认值 |
| 锁定状态 | 清除人工锁定覆盖，按当前日期重新计算 `lockStatus` |
| 项目元数据 | 重导时清除 `_field_change_log`、`_changed_fields` 等，避免脏数据影响 Luckysheet 与变更高亮 |

- **演示新增项目：** 初始数据约 20 条时固定为 `B24194-0078`、`B24264-0039`、`B25001-0044`、`B25126-0002`、`B25126-0004`；大库则均匀抽样 5 条，写入 `meta.demoNewProjectNos`。
- **实现：** `server/dev-reset-seed.js`（`seedDevEnvironment`），由 `reset-dev`、`reseed`、空库首次导入调用。
- **与管理页差异：** 管理设置中的「从初始 Excel 恢复」（`POST /api/admin/reseed`）**保留当前**报告月份等配置，仅重导项目并清空快照/审计，但会**同样**自动重建上月对比快照；开发重置会**连同周期配置一并恢复默认**。
- **前端：** 成功后 `Store.resetDevEnvironment()` 应用返回的 `state` 并 **整页刷新**，确保 Luckysheet 等视图与库一致。

---

## 3. 业务规则与逻辑

### 3.1 数据源定义
| 数据类型 | 数据源 | 说明 |
|---|---|---|
| **项目基本信息** | **CRB (合同系统)** | 包括项目名称、经理、时间、客户等，作为基础只读数据。 |
| **总合同额** | **CRB (合同系统)** | **核心真理值**：系统自动抓取 CRB 数据。未签署项目取 **CRB1**；签署后（状态变为“已确认”）自动切换取 **CRB3**。前端不可编辑。 |
| **当年调整值** | **系统计算** | **差额指标**：系统自动计算 `O = P - N`。仅用于展示当年合同额相对于历史基准的变化情况。 |
| **截止上一年底** | **系统历史库** | **基准锁定**：每年 1 月 1 日系统从 CRB 快照生成并锁定，不再随 CRB 变化。 |
| **项目时间** | **CRB** | **严禁手动修改**，以 CRB 为准，解决线下填报随意改时间的问题。 |
| **回款/现金流数据** | **财务系统** | **独立取数**，不依赖开票数据。解决“未开票但已回款”无法体现的问题。 |
| **开票金额** | **财务系统（当月实际）/ 手工预测（未来月）** | 报告月**当月**开票由财务系统同步实际发生值（只读）；**之后月份**为 PM 填报的预测值，可在线编辑。 |
| **回款金额** | **财务系统（当月实际）/ 手工预测（未来月）** | 同开票：当月实际值由财务系统同步（只读）；之后月份为预测值可编辑。 |
| **存量开票额（R）** | **系统计算** | `P − AC`；小于 0 为**预警**（累计开票超合同），见 **§2.7.1**。 |
| **存量合同额（S）** | **系统计算** | `P − U`；**不得为负**；约束月度完成额累计，见 **§2.7.1**。 |
| **合同签署状态** | **CRB** | 系统自动判定：若 CRB 状态为“已确认”则为“已签署”，否则为“未签署”。 |

### 3.2 项目准入与“提前申报产值”规则
- **正常准入**：所有**已签署合同**的项目自动进入追踪表。
- **提前申报产值（未签署项目）**：
  1.  **发起**：针对尚未签署合同但已开展工作需申报产值的项目，由项目经理发起“提前申报产值”流程。
  2.  **审批**：审批通过后，该项目**允许进入**当期追踪表，此时“合同是否签署”字段显示为“未签署”。
  3.  **状态联动**：若后续该项目在 CRB 中完成签署（状态变更为“已确认”），系统自动将追踪表中该项目的状态更新为“已签署”。
- **数据隔离**：未签署且未通过提前申报审批的项目，**不可见**于追踪表中。

### 3.2 产值申报规则
- **填报依据：** 允许多种方式并行（形象进度、人工时比例、设计进度交付物等），由项目经理自行判断。
- **闭环要求：** 过程允许偏差，但**最终累计完成合同额必须 = 合同总额**。
- **存量合同额约束（已实现）：** 各月完成额填报后，**存量合同额（S = P − U）不得为负**；调增完成额导致 S 更负时系统拒绝保存；提交前须全部修复为 S ≥ 0（含 CRB 合同额下调后的存量修复场景，见 **§2.7.1**）。
- **存量开票额提示：** **存量开票额（R = P − AC）** 为负时仅作**预警**展示（红底红字），不阻断提交，便于发现累计开票超合同情况。
- **新/旧项目自动判定：** 根据合同签署年份自动标记：
  - `新项目`：当年新签。
  - `旧项目`：往年签约延续。

### 3.3 财务与 WIP 规则
- **预收款定义：** **未开票但已收到的款项**（与合同是否签署无关）。
  - 已开票 + 回款 = 正常回款
  - 未开票 + 回款 = 预收款
- **WIP 账龄分段：** <1个月、1~3个月、3~6个月、6~12个月、1~2年、2~3年、>3年。
- **WIP 成因分类：** A(到节点未付)、B(单价合同未到节点)、C(总价合同未到节点)、D(其它)。

---

### 3.6 动态年份与历史归档机制
针对跨年场景，系统需实现 **"年份自动翻转 (Year-End Rollover)"** 与 **"历史数据沉淀"**：

1.  **字段名动态化**：
    *   界面显示不再包含具体年份（如"25 年底"），改为相对年份描述：
        *   `截止上一年底` (Cut-off Previous Year End)
        *   `当年调整值` (Current Year Adjustment)
        *   `当年期初存量` (Current Year Backlog)
    *   每年 1 月 1 日（或管理员手动触发），系统自动计算当前 `SystemYear`，并将界面上的"上一年底"指向上一年的数据。

2.  **跨年自动结转逻辑**：
    *   **基准更新**：跨年瞬间，系统自动抓取当年的 `始累完成合同额 `、`始累开票`、` 始累回款` 数值，将其"冻结"为下一年度的 `截止上一年底` 基准值。
    *   **调整值重置**：新年的 `当年调整值` 初始归零，允许项目经理重新填报新一年的变更。

3.  **历史数据持久化存储**：
    *   虽然前端追踪表只展示"上一年底"的切片数据，但在**数据库后台**必须保留每一年的历史版本。
    *   **表结构设计**：增加 `Project_Yearly_Baseline` 表，记录 `(项目 ID, 年份，N 值，T 值，AA 值，AD 值)`。
    *   支持随时回溯查询某项目在特定年份的基准数据，用于审计和历史对比。

### 2.16 补充需求确认（7 项，2026-05-20 定稿）

| # | 补充项 | 状态 | 说明 |
|---|---|---|---|
| 1 | **单元格底色区分（数据源可视化）** | ✅ 采纳 | 在变更高亮（`#fff7ed`）和新增项目底色（`#d9e7d8`）基础上，新增「CRB 同步列」浅灰底（`#f5f5f5`）与「财务系统同步列」浅蓝底（`#e8f4fd`），一眼区分数据归属。公式计算列（`auto_calc`）保持当前只读灰色。 |
| 2 | **双击编辑面板** | ✅ 采纳（有条件） | 文本/数字/金额列保持 Luckysheet 内联编辑；**枚举型字段**（项目实施进展 M、WIP 成因 TQ、风险等级 TW 等）和**长文本**（WIP 分析说明 TR、备注等）改为双击弹出编辑面板，提升输入体验。 |
| 3 | **预收款核销模块** | ✅ 采纳 | 增加预收款→开票核销流水表（`prepayment_writeoffs`：项目号、核销金额、核销日期、关联发票号、核销人）。财务审核角色在 1-3 日专属期操作，核销记录写入审计日志。核销后自动减少对应项目的"未开票预收款"余额。 |
| 4 | **汇率差调整** | ✅ 采纳 | 对涉外项目（合同币种 ≠ CNY），在合同额区块新增「汇率差调整值」列（`FX_ADJ`），仅板块管理员/财务审核/系统管理员可编辑。影响当年调整值计算：`当年调整值 = 总合同额(含税) - 截止上一年底 + 汇率差调整`。 |
| 5 | **Save / Submit 分离** | ✅ 已实现 | §2.12.2 已描述「保存」（刷盘不触发流程）与「提交」（触发快照/审批流），无需新增开发。 |
| 6 | **批量提醒（一键催办）** | ✅ 采纳 | 每月 19 日自动提醒基础上，板块管理员可在填报页手动触发「一键催办」，向本板块 `status !== submitted` 且近 7 日无编辑记录的 PM 发送站内消息/邮件。系统管理员可全公司范围触发。 |
| 7 | **业务批注（非变更批注）** | ✅ 采纳 | 允许任意可编辑角色在任意可编辑单元格添加业务备注（如"客户变更需求，产值延后"），存储于 `_field_notes`，与 `_field_change_log` 独立。批注不触发高亮，仅以小角标提示。变更批注（橙色）与业务批注（蓝色）角标颜色区分。 |
| 8 | **存量指标 R/S 预警与校验** | ✅ 已实现 | R 列更名「存量开票额」、S 列更名「存量合同额」；R/S 为负时红底红字 + 表格工具栏「预警」筛选；S 为负时禁止调增完成额并阻断 PM/板块提交。详见 **§2.7.1**、`js/stock-validation.js`。 |
| 9 | **表格工具栏与紧凑列** | ✅ 已实现 | 关闭 Luckysheet 内置顶栏；Luckysheet 上方 `.sheet-toolbar` 承载项目筛选 +「仅显示项目信息与可编辑列」；`lang: 'zh'`。详见 **§2.8.6**、**§2.12.1**。 |

### 2.17 角色字段级权限矩阵（6 角色 × 83 字段）

#### 权限规则汇总

| 字段来源 | 可编辑角色 | 说明 |
|---|---|---|
| **system_sync**（CRB/财务同步） | 全部 R | 不可编辑，系统自动同步 |
| **auto_calc**（公式计算） | 全部 R | 不可编辑，系统自动计算 |
| **manual_input（通用）** | 系统管理员 W · 板块管理员 W · 项目经理 W（仅本人项目） | 总监/群主/财务 R |
| **manual_input（财务相关）** | 同上 + **财务审核 Wf**（1-3 日专属期） | 含开票列、回款列、预收款、汇率差 |
| **manual_input（WIP 分析）** | 系统管理员 W · 板块管理员 W · 项目经理 W | 财务/总监/群主 R |

- [ ] **成本法预留：** 目前不具备成本法条件（无预算、无精确成本），是否预留接口？
- [ ] **WIP 报告流转：** 报告生成后是仅做通报，还是需要相关负责人回复措施？
- [ ] **时间窗配置后台：** 确认 19/25/9 动态调整的前端交互形式与权限边界（仅系统管理员可改）。

---

## 5. 数据范围与背景

### 5.1 项目范围
- **覆盖范围：** 公司 **所有已签署合同的项目** + **提前申报产值的项目**
- **涉及中心：** 线上化系统需覆盖 **所有项目执行中心**（参考 PM 知识库），各中心分别填报，最终汇总。

### 5.2 字段理解纠正

| 字段 | 之前的理解 | 纠正后 |
|---|---|---|
| **项目执行单位编码** | 源表多为 `S520` 等 | 系统注册 **12 个** SAS 执行单位（§2.15）；短码归一为 `SAS*`（如 `S520` → `SAS520`） |
| **新、旧项目** | 区分历史遗留 vs 新增 | 区分是否为 **当年新签的项目**（"新项目" = 当年新签约，"旧项目" = 往年签约延续至今） |
| **R 列 / S 列** | 总合同额-开票 / 总合同额-完成 | 已更名为 **存量开票额（R）** / **存量合同额（S）**；分区名 **存量指标**（§2.7.1） |

---

*最后更新：2026-05-20 | 本文档由 Ethan 与比比 🐸 共同维护*

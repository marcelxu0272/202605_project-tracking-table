# 项目追踪表线上化 — 需求文档

> **关联文档：** [字段字典.md](./字段字典.md) | [会议记录精修版](./产值报告线上化讨论_精修版.md) | [关键要点](./产值报告线上化讨论_关键要点.md)  
> **原始数据：** `S520_金山中心_项目执行跟踪详细数据2026年05月.xlsx`  
> **最后更新：** 2026-05-19

---

## 1. 角色定义

系统共设 **6 个角色**，权限分工如下：

| 角色 | 定位 | 编辑权限 | 审批权限 | 说明 |
|---|---|---|---|---|
| **系统管理员** | 系统运维 | ✅ | ❌ | 负责系统配置、用户管理、权限分配、数据锁定/解锁、报表生成配置等；开发环境下可使用「重置为初始状态」恢复全库（§2.10）。 |
| **财务审核** | 数据校验 | ✅ | ❌ | **每月 1-3 日专属权限**：可对全公司的开票、回款、预收款等财务相关列进行统一核对与修正。 |
| **板块管理员** | 数据管理 | ✅ | ❌ | 负责板块内数据汇总校验、维护 CRB/财务数据映射关系、异常数据处理。 |
| **项目经理** | 数据填报 | ✅ | ❌ | 负责本人管辖项目的产值、进度、WIP 分析填报，Excel 导入/导出。 |
| **板块总监** | 审批决策 | ❌ | ✅ | 查看板块数据，审批确认填报结果。 |
| **项目群群主** | 审批决策 | ❌ | ✅ | 查看项目群数据，审批确认填报结果。 |

---

## 2. 核心功能需求

### 2.1 填报与交互模式
- **在线填报：** 支持在网页端直接修改单元格数据（基于 **Luckysheet**，类 Excel）。
- **填报视图（当前实现）：** 填报页**仅展示 Luckysheet**，为默认且唯一可见的填报界面；**经典 HTML 表格**实现代码保留于 `ProjectEditor.js`，通过 `showLegacyHtmlTable` 开关控制（默认 `false`，UI 不提供切换入口），便于开发对照或临时启用。
- **工具栏视图筛选：** 保留「全部 / 新增项目 / 有变更」筛选（与 §2.7 联动），与「填报视图」切换无关。
- **Excel 导入/导出：**
  - 支持下载模板（包含已锁定的基础数据列）。
  - 支持用户上传填好的 Excel，系统自动解析更新。
  - **权限控制：** 无论是线上还是 Excel，均需实现**细粒度的单元格权限控制**（只读 vs 可编辑）。
- **信息折叠与排序：**
  - 基本信息等辅助信息可折叠，减少视觉干扰。
  - **排序优化：** 当月有数据变动（工时/产值/开票/回款）的项目自动置顶，无变动项目置后。

### 2.2 数据同步与刷新机制
- **实时刷新：** 系统平时保持与 CRB（合同系统）和财务系统的数据实时同步。
- **锁定期管理：**
  - 填报期开启时，管理员可**手动锁定数据快照**。
  - 锁定期间，CRB/财务数据变更不自动覆盖填报数据，确保基准一致。
  - 填报结束审批后，解锁并更新数据。

### 2.3 自动化报表
- **WIP 分析报告：** 每月自动生成 PDF 格式的 WIP 分析报告（账龄分析、风险预警、改进建议）。
- **分层输出：** 报告按板块级、项目群级、公司级分别生成。
- **预警通知：** 当 **WIP > 合同额 50%** 时，系统自动向对应中心和板块发送预警。

### 2.4 填报周期与锁定机制 (动态可配置)
- **1 日～3 日：财务专属期** 开放**财务审核**角色权限，针对开票、回款、预收款等列进行全量数据校验与修正。
- **19日：填报提醒** 系统自动发送提醒邮件/消息，督促各角色在平台完成当月数据填写。
- **25日：月度锁定** 填报窗口关闭，数据冻结。除系统管理员外，任何人不可编辑。
  - *管理员特权：* 系统管理员可针对特定项目临时解锁并修改，操作全程留痕。
- **次月9日：常规解禁** 锁定解除。板块管理员与项目经理可随时修改“可编辑权限”内的字段。
- **数据初始化规则 (每月开启填报时自动执行)**：
  - **当月清零**：当月（Reporting Month）的 **完成额、开票额、回款额** 自动重置为 `0`，等待填报。
  - **历史保留**：过往月份的数据保持只读并完整显示。
  - **未来透视**：未来月份的预测数据（Forecast）自动继承上一轮填报值，允许编辑修改。
- **动态调整：** 上述三个时间节点（19/25/9）必须在系统后台支持**动态配置**，以应对节假日或临时调整。

### 2.5 数据变更追踪 (Audit Log)
- **全量记录：** 任何数据的增、删、改（含在线编辑、Excel 导入、管理员解锁修改）均自动记录变更日志。
- **日志维度：** 操作人、操作时间、所属项目、修改字段、原值、新值、修改原因（可选）。
- **审计支持：** 支持按项目、人员、时间范围查询导出，满足财务与运营审计要求。

### 2.6 审批流程与版本快照 (Snapshot Management)
每月填报数据提交后，进入多节点审批流，每次状态变更自动生成**月度快照版本**：
1. **提交生成 Draft 版**：各板块/中心完成填报并提交 -> 系统锁定当前数据，生成 `Draft`（草稿版）快照。
2. **总监审批生成 Approve1 版**：板块总监审核通过 -> 生成 `Approve1` 版快照。
3. **群主审批生成 Approve2 版**：项目群群主审核通过 -> 生成 `Approve2` 版快照。
4. **管理员确认生成 J 版**：系统管理员最终核对确认 -> 生成 `J 版`（正式归档版）快照。
- **版本对比：** 支持任意两个版本之间的数据差异对比（Diff），支持高亮显示变动单元格。
- **驳回机制：** 任一审批节点驳回，数据状态退回 `Draft` 或指定节点，允许修改后重新流转。

### 2.7 变更高亮机制 (Diff Highlighting)
为提升填报与审批效率，系统需自动识别并视觉强化本月变动数据：
- **🆕 新增项目整行高亮**：对比上月快照，当月**首次进入跟踪范围**的项目，整行应用高亮背景（推荐品牌色浅底 `rgba(0,112,105,0.1)`）。
- **📝 变更单元格高亮**：
  - **当月变更**：当月值与上月快照不一致的字段（含新填报、修正、系统同步更新）。
  - **未来预测变更**：修改了未来月份的预测数据（Forecast），同样视为变更并高亮显示。
  - **视觉标识**：对应数据块/单元格顶部添加色条或左侧边框标识。审批视图默认开启此高亮。
- **⚡ 联动计算高亮**：因手工修改导致**自动计算的字段**（如 WIP、累计回款、差值等）发生数值变化时，该计算字段单元格同步触发高亮，确保用户感知数据变动。
- **👁️ 视图过滤控制**：在填报页与审批看板顶部提供切换按钮：`显示全部` / `仅看新增项目` / `仅看变更数据`，方便用户聚焦本月工作内容。
- **⚙️ 技术实现**：基于月度快照表进行行级与字段级 `Diff` 计算，前端通过动态 CSS 类渲染。

---

### 2.8 Luckysheet 表格布局与列对齐（填报页）

与源 Excel（S520，83 列字段字典）保持一致，避免「代码里的 E 列」与「界面上看到的 E 列」不一致。变更高亮、工具栏视图筛选等交互要求见 **§2.7**；经典 HTML 表逻辑与 Luckysheet 对齐，但默认不向用户展示（见 **§2.1**）。

#### 2.8.1 列号对齐原则（已确认）

| 约定 | 说明 |
|---|---|
| **无额外序号列** | 不在数据区左侧增加「# / 序号」列；Luckysheet 行号由组件自带行头展示即可。 |
| **列字母 = 字段字典** | 字段字典中的列号（A、B、E、F…）与 Luckysheet 表头列字母、公式引用列字母**一一对应**（A→索引 0，E→索引 4）。 |
| **经典 HTML 表同步** | 经典表格视图同样不展示序号列，列顺序与字段字典一致。 |
| **公式引用** | Luckysheet 内 `auto_calc`、小计/合计行公式均按字段字典列字母书写（如 `E5` 表示字段 E、第 5 行），不再对全表做 +1 偏移。 |

#### 2.8.2 行布局（0-based，Luckysheet）

| 行号 | 用途 |
|---|---|
| 0 | **小计**（Subtotal）：金额列使用 `SUBTOTAL(9, 数据区)` |
| 1 | **合计**（Total）：金额列使用 `SUM(数据区)`；标签文案显示在 **G 列**（「小计 Subtotal」「合计 Total」） |
| 2 | 大类 / 分区标题行（按 `section` 合并单元格） |
| 3 | 字段中文标题行 |
| 4+ | 项目数据行（顺序与筛选结果一致） |

小计/合计置于大分类之上，**默认筛选**仅覆盖「字段标题行 + 数据行」，避免汇总行被筛掉。

- **横向滚动与冻结列：** 筛选按钮挂在可滚动的 `#luckysheet-cell-main` 内。横向滚动时对 **A–F 冻结列** 的按钮按 `left = 基准left + scrollLeft` 补偿，使其与冻结表头对齐；对滚入冻结区下方的可滚动列按钮则 `display:none` 隐藏。

#### 2.8.3 冻结与表头

- **冻结窗格：** 冻结至字段标题行（含小计、合计、大分类行）及 **F 列（项目号）** 左侧，横向滚动时保留项目识别关键列（A–F）。
- **表头展示：** 仅显示字段**中文名**，不在表头第二行重复展示列字母（如 `(E)`）。
- **可编辑列表头：** 当前角色、锁定状态下**可编辑**的列，表头使用浅黄底 + 深黄字区分（与只读列表头对比）。

#### 2.8.4 列宽与自定义宽度

- **列宽配置：** 支持按**字段字典列字母**配置窄列（如 E/F/G）；写入 Luckysheet `config.columnlen` 时键为**列索引**（与列字母一致映射）。
- **防止被自动撑开：** 对已配置 `columnlen` 的列，须同步设置 `config.customWidth` 为**按列索引的对象**（如 `{ "4": 1 }`），避免网格刷新后按单元格内容重算列宽导致配置失效。
- **默认列宽：** 未单独配置的列按字段 `colWidth` 或合理默认宽度（约 72–220px）处理。

#### 2.8.5 填报期内的列级只读（动态时间窗）

- 以系统**报告月**（如 `2026-05`）为基准：**报告月之前**的月份列（月度完成 AV–BG、对应开票/回款列等）在填报期内为**只读**，仅允许编辑当月及未来预测列。
- Luckysheet 通过工作表保护 + 单元格 `lo` 标记 + 编辑前钩子二次校验，与 `field-config` 中 `canEditField` / `isPastReportingMonthField` 规则一致。
- 经典 HTML 表对同期列应用相同只读样式（如 `month-locked-cell`）。

#### 2.8.6 数据验证与其它

- **下拉枚举：** 对含 `enum_values` 且当前可编辑的单元格，下发 Luckysheet 数据验证（dropdown）；选项文本内含英文逗号时跳过该校验格，避免解析错误。
- **公式链：** 创建 sheet 时配置 `calcChain`（先数据行公式，再小计/合计行），并在加载后触发公式重算，保证 SUBTOTAL/SUM 与 `auto_calc` 正确显示。
- **默认筛选：** 表创建后对「字段标题行 + 数据行」范围开启筛选（`filter_select` + `setRangeFilter('open')`），小计/合计/大分类行在筛选区外。

#### 2.8.7 提交审批后的填报锁定

- **触发（板块级）：** 板块管理员点击「提交审批」并成功生成 **Draft** 快照后，系统设置 `reportingSubmitted = true`（持久化于 SQLite `meta`）。
- **填报页：** 「提交审批」按钮禁用并显示「已提交审批」；Luckysheet 与经典 HTML 表（若启用）在**非系统管理员**角色下进入只读，与锁定期规则叠加。
- **解除：** 审批驳回、管理员在管理页「从初始 Excel 恢复」或开发环境「重置为初始状态」时清除该标记；系统管理员在已提交状态下仍可编辑。

---

### 2.11 项目经理提交与板块接收

项目经理的「提交」是面向板块管理员的内部汇报动作，不等同于进入审批流的 Draft 提交。

#### 角色页面权限

| 角色 | 可见页面 |
|---|---|
| **项目经理** | 数据看板、填报表格（隐藏审批流程、审计日志、管理设置） |
| **板块管理员** | 全部页面 |

- 项目经理登录后默认进入数据看板；看板和填报表格的数据均按 **`pm_name === 当前用户`** 过滤，仅展示本人负责项目。
- 模拟项目经理账号：**何孝刚**（5 条项目）、**宋建生**（2 条项目），与数据库 `pm_name` 字段一致。

#### 提交状态机

```
[可编辑] ──提交──> [submitted: 待板块接收，PM 锁定]
                         │
              板块管理员确认接收
                         │
[可编辑] <──received────┘
（板块正式提交审批前可重复上述循环）
                         │
              板块管理员提交审批（生成 Draft）
                         │
                  [板块锁定，PM 不可再提交]
```

#### 数据存储

- `meta.pmSubmissions`：按报告月分桶，记录每个 PM 的提交状态（`submitted` / `received`）、提交时间、对应快照版本及项目号列表。
- PM 子集快照（`snapshots` 表，版本名 `PM:<姓名>:<月>:<时间戳>`）：仅含该 PM 当时负责的项目，供板块管理员 diff 对比。
- `resetDevMeta` / `reseed` 时同步清空 `pmSubmissions` 与 PM 子集快照。

#### 板块管理员操作

- 填报页工具栏下方显示**待接收面板**，列出本月 `status === 'submitted'` 的 PM（姓名、项目数、提交时间）。
- 「查看变更」：对比提交快照 vs 当前库，展示字段级 diff。
- 「确认接收」：将状态改为 `received`，PM 解锁可再次编辑提交；写审计日志。
- 板块管理员汇总确认所有项目后，点击「提交审批」生成全局 Draft，进入原有四节点审批流。

---

### 2.9 主布局：侧边栏折叠

- **折叠/展开：** 侧栏底部与顶栏左侧各提供折叠按钮（`el-icon-s-fold` / `el-icon-s-unfold`），宽度在展开（约 220px）与折叠（约 64px）间切换。
- **折叠态：** Logo 缩为「金」字图标；导航仅图标，悬停显示菜单标题（Element UI `el-menu :collapse`）；底部填报期状态缩为圆点 + Tooltip。
- **状态记忆：** `sidebarCollapsed` 写入浏览器 `localStorage`（`ptrack_sidebar_collapsed`），刷新后保持。

### 2.10 开发测试：环境重置

> 面向本地/联调环境，便于反复验证填报与审批流程；**上线前建议隐藏或仅开放给系统管理员**。

- **入口：** 顶栏右上角用户下拉菜单 → **「重置为初始状态（开发）」**（二次确认）。
- **接口：** `POST /api/admin/reset-dev`（需重启 `npm start` 后加载的最新服务端）。
- **重置范围：**

| 项 | 行为 |
|---|---|
| 项目数据 | 从项目根目录「初始数据.xlsx」（或 S520 源表 / `PTRACK_INIT_XLSX`）重新导入，覆盖当前库 |
| 审批流程 | `approvalStatus` → `draft`，`reportingSubmitted` → `false` |
| 快照与审计 | 清空全部版本快照与审计日志 |
| 填报周期 | `periodConfig`、`reportingMonth` 恢复 `DEFAULT_PERIOD_CONFIG` 默认值 |
| 锁定状态 | 清除人工锁定覆盖，按当前日期重新计算 `lockStatus` |

- **与管理页差异：** 管理设置中的「从初始 Excel 恢复」（`POST /api/admin/reseed`）**保留当前**报告月份等配置，仅重导项目并清空快照/审计；开发重置会**连同周期配置一并恢复默认**。
- **前端：** 成功后 `Store.resetDevEnvironment()` 应用返回的 `state` 并 **整页刷新**，确保 Luckysheet 等视图与库一致。

---

## 3. 业务规则与逻辑

### 3.1 数据源定义
| 数据类型 | 数据源 | 说明 |
|---|---|---|
| **项目基本信息** | **CRB (合同系统)** | 包括项目名称、经理、时间、客户等，作为基础只读数据。 |
| **总合同额** | **CRB (合同系统)** | **核心真理值**：系统自动抓取 CRB 数据。未签署项目取 **CRB1**；签署后（状态变为“已确认”）自动切换取 **CRB3**。前端不可编辑。 |
| **当年调整值** | **系统计算** | **差额指标**：系统自动计算 `O = P - N`。仅用于展示当年合同额相对于历史基准的变化情况。 |
| **截止上一年底** | **系统历史库** | **基准锁定**：每年 1 月 1 日系统从 CRB 快照生成并锁定，不再随 CRB 变化。 |
| **项目时间** | **CRB** | **严禁手动修改**，以 CRB 为准，解决线下填报随意改时间的问题。 |
| **回款/现金流数据** | **财务系统** | **独立取数**，不依赖开票数据。解决“未开票但已回款”无法体现的问题。 |
| **开票金额** | **手工填报 / 财务核对** | 业务人员逐月填报预测值，财务审核角色在每月 1-3 日可编辑核对实际值。 |
| **合同签署状态** | **CRB** | 系统自动判定：若 CRB 状态为“已确认”则为“已签署”，否则为“未签署”。 |

### 3.2 项目准入与“提前申报产值”规则
- **正常准入**：所有**已签署合同**的项目自动进入追踪表。
- **提前申报产值（未签署项目）**：
  1.  **发起**：针对尚未签署合同但已开展工作需申报产值的项目，由项目经理发起“提前申报产值”流程。
  2.  **审批**：审批通过后，该项目**允许进入**当期追踪表，此时“合同是否签署”字段显示为“未签署”。
  3.  **状态联动**：若后续该项目在 CRB 中完成签署（状态变更为“已确认”），系统自动将追踪表中该项目的状态更新为“已签署”。
- **数据隔离**：未签署且未通过提前申报审批的项目，**不可见**于追踪表中。

### 3.2 产值申报规则
- **填报依据：** 允许多种方式并行（形象进度、人工时比例、设计进度交付物等），由项目经理自行判断。
- **闭环要求：** 过程允许偏差，但**最终累计完成合同额必须 = 合同总额**。
- **新/旧项目自动判定：** 根据合同签署年份自动标记：
  - `新项目`：当年新签。
  - `旧项目`：往年签约延续。

### 3.3 财务与 WIP 规则
- **预收款定义：** **未开票但已收到的款项**（与合同是否签署无关）。
  - 已开票 + 回款 = 正常回款
  - 未开票 + 回款 = 预收款
- **WIP 账龄分段：** <1个月、1~3个月、3~6个月、6~12个月、1~2年、2~3年、>3年。
- **WIP 成因分类：** A(到节点未付)、B(单价合同未到节点)、C(总价合同未到节点)、D(其它)。

---

### 3.6 动态年份与历史归档机制
针对跨年场景，系统需实现 **"年份自动翻转 (Year-End Rollover)"** 与 **"历史数据沉淀"**：

1.  **字段名动态化**：
    *   界面显示不再包含具体年份（如"25 年底"），改为相对年份描述：
        *   `截止上一年底` (Cut-off Previous Year End)
        *   `当年调整值` (Current Year Adjustment)
        *   `当年期初存量` (Current Year Backlog)
    *   每年 1 月 1 日（或管理员手动触发），系统自动计算当前 `SystemYear`，并将界面上的"上一年底"指向上一年的数据。

2.  **跨年自动结转逻辑**：
    *   **基准更新**：跨年瞬间，系统自动抓取当年的 `始累完成合同额 `、`始累开票`、` 始累回款` 数值，将其"冻结"为下一年度的 `截止上一年底` 基准值。
    *   **调整值重置**：新年的 `当年调整值` 初始归零，允许项目经理重新填报新一年的变更。

3.  **历史数据持久化存储**：
    *   虽然前端追踪表只展示"上一年底"的切片数据，但在**数据库后台**必须保留每一年的历史版本。
    *   **表结构设计**：增加 `Project_Yearly_Baseline` 表，记录 `(项目 ID, 年份，N 值，T 值，AA 值，AD 值)`。
    *   支持随时回溯查询某项目在特定年份的基准数据，用于审计和历史对比。

- [ ] **角色字段级权限矩阵：** 具体定义每个角色对 83 个字段的读/写权限（待 Ethan 确认）。
- [ ] **成本法预留：** 目前不具备成本法条件（无预算、无精确成本），是否预留接口？
- [ ] **WIP 报告流转：** 报告生成后是仅做通报，还是需要相关负责人回复措施？
- [ ] **时间窗配置后台：** 确认 19/25/9 动态调整的前端交互形式与权限边界（仅系统管理员可改）。

---

## 5. 数据范围与背景

### 5.1 项目范围
- **覆盖范围：** 公司 **所有已签署合同的项目** + **提前申报产值的项目**
- **涉及中心：** 线上化系统需覆盖 **所有项目执行中心**（参考 PM 知识库），各中心分别填报，最终汇总。

### 5.2 字段理解纠正

| 字段 | 之前的理解 | 纠正后 |
|---|---|---|
| **项目执行单位编码** | 固定为 S520 | 线上化后会有多个执行中心各自的编码（详见 PM 知识库） |
| **新、旧项目** | 区分历史遗留 vs 新增 | 区分是否为 **当年新签的项目**（"新项目" = 当年新签约，"旧项目" = 往年签约延续至今） |


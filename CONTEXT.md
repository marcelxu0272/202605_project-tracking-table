# 项目追踪表

项目追踪表汇集项目执行、合同、产值、开票、回款和经营分析数据，并以统一字段口径支持填报、审批与月度归档。

## 字段取值

**公式字段**：
由系统按固定公式计算的字段，所有用户均只读。
_Avoid_: 自动同步字段、可覆盖公式

**人工维护字段**：
外部业务系统不提供完整可靠数据、初始化后由获授权业务人员维护的字段。
_Avoid_: 不同步字段、静态字段

**系统直取字段**：
业务系统是唯一权威来源，业务事件发生后直接更新且任何用户不可编辑的字段。
_Avoid_: 引用列、可覆盖系统列

**系统调整字段**：
显示值由系统值与人工调整值相加形成的字段；系统值始终保留业务系统原值，人工修改只改变调整值。
_Avoid_: 管理员覆盖字段、锁死系统值

**系统值**：
由源业务系统事件写入、不可人工修改的原始值。
_Avoid_: 显示值、覆盖值

**调整值**：
获授权用户为修正展示口径而维护的差额，独立于系统值并在后续系统更新时继续保留。
_Avoid_: 系统值、覆盖值

**显示值**：
用户在表格中看到且供下游公式使用的最终值；系统调整字段的显示值等于系统值加调整值。
_Avoid_: 系统值

## 数据更新

**业务事件更新**：
合同额变更、开票、回款、项目经理变更等业务动作完成后，由源业务系统主动触发的字段更新。
_Avoid_: 手动刷新、每日轮询同步

**数据冻结窗口**：
月末至次月归档恢复时点之间暂停业务事件写入的期间，人工维护与调整按权限继续进行。
_Avoid_: 填报锁定

# 技术栈与开发规范

> **项目：** 产值报告线上化  
> **最后更新：** 2026-05-13

---

## 一、开发原则

### 1.1 轻量级架构 (No Build Tools)
- **核心思路：** 基于 HTML5/CSS3/JavaScript 开发，**不依赖 Webpack/Vite/NPM 等构建工具链**。
- **实现方式：** 所有依赖通过 CDN `<script>` 或 `<link>` 标签引入，双击 `index.html` 即可运行。
- **优势：** 部署极简（只需静态文件托管），环境配置零成本，适合企业内部快速迭代工具。

### 1.2 依赖管理
- **CDN 优先：** 所有第三方库通过 CDN 引入，避免本地 `node_modules` 的复杂性。
- **版本锁定：** 明确指定 CDN 版本号，防止线上自动更新导致的不兼容问题。

### 1.3 文档驱动
- **MCP Context7：** 编写代码前，优先使用 MCP 工具 **Context7** 获取对应技术栈的最新官方文档与最佳实践，避免依赖过时记忆。

### 1.4 架构设计
- **高度模块化：** 尽管无构建工具，仍需保持代码结构清晰。
  - 按业务功能拆分 JS 文件（如 `project-list.js`, `wip-analysis.js`）。
  - 使用 ES6 `import/export`（配合 `<script type="module">`）或 IIFE 隔离作用域。
  - Vue 组件尽量使用 `Vue.component` 或单文件组件的 CDN 替代方案（如 `http-vue-loader` 或内联模板）。

### 1.5 视觉规范
- **PC 优先：** 专注主流桌面分辨率适配（1920×1080, 1366×768），**不考虑移动端响应式**。
- **遵循设计文档：** 在没有特殊要求的情况下，设计规范请严格参考 DESIGN.md
- **禁止：** 大量渐变元素、花哨动画、过度装饰。
- **图标：** 使用 SVG/字体图标（如 ElementUI 内置图标），**禁用 Emoji 替代 Icon**。

---

## 二、技术栈清单

| 技术 | 版本 | 用途 | 说明 |
|:---|:---|:---|:---|
| **Vue.js** | `2.5.22` | 核心框架 | 采用 Global Build，通过 CDN 引入。提供数据绑定与组件化能力。 |
| **Vue Router** | `3.0.2` | 路由管理 | 与 Vue 2 配套，实现 SPA 页面切换（如：填报页、审批页、看板页）。 |
| **Element UI** | `2.15.x` | UI 组件库 | 提供 Table、Form、Dialog、Pagination 等企业级组件，开箱即用。 |
| **Luckysheet** | `2.1.13` | 在线表格引擎 | 实现高保真 Excel 还原、表单在线填写及**细粒度单元格权限控制**。 ([GitHub](https://github.com/dream-num/Luckysheet)) |
| **Tailwind CSS** | `3.x (CDN)` | 原子化 CSS | 通过 Play CDN 引入，用于快速布局与样式覆盖，减少自定义 CSS。 |
| **Apache ECharts** | `5.4.3` | 数据可视化 | 用于产值趋势图、WIP 账龄分布图、回款饼图等图表展示。 |

---

## 三、CDN 依赖引入示例

以下 `index.html` 头部可直接作为项目模板：

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>产值报告线上化平台</title>
  
  <!-- Tailwind CSS (Play CDN) -->
  <script src="https://cdn.tailwindcss.com"></script>
  
  <!-- Element UI CSS -->
  <link rel="stylesheet" href="https://unpkg.com/element-ui/lib/theme-chalk/index.css">
  
  <!-- Luckysheet CSS & Dependencies -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@3.3.7/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/luckysheet@latest/dist/plugins/css/pluginsCss.css" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/luckysheet@latest/dist/plugins/plugins.css" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/luckysheet@latest/dist/css/luckysheet.css" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/luckysheet@latest/dist/assets/iconfont/iconfont.css" />
  
  <style>
    /* 全局商务风格覆盖 */
    body { font-family: "Helvetica Neue", Helvetica, "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", Arial, sans-serif; }
  </style>
</head>
<body>
  <div id="app"></div>

  <!-- jQuery (Luckysheet 依赖) -->
  <script src="https://cdn.jsdelivr.net/npm/jquery@3.6.0/dist/jquery.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
  
  <!-- Vue 2.5.22 -->
  <script src="https://cdn.jsdelivr.net/npm/vue@2.5.22/dist/vue.js"></script>
  
  <!-- Vue Router 3.0.2 -->
  <script src="https://unpkg.com/vue-router@3.0.2/dist/vue-router.js"></script>
  
  <!-- Element UI -->
  <script src="https://unpkg.com/element-ui/lib/index.js"></script>
  
  <!-- Luckysheet JS -->
  <script src="https://cdn.jsdelivr.net/npm/luckysheet@latest/dist/plugins/js/plugin.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/luckysheet@latest/dist/luckysheet.umd.js"></script>

  <!-- ECharts 5.4.3 -->
  <script src="https://cdn.jsdelivr.net/npm/echarts@5.4.3/dist/echarts.min.js"></script>

  <!-- 业务逻辑 -->
  <script src="./js/app.js"></script>
</body>
</html>
```

---

## 四、项目结构建议

```text
产值报告线上化/
├── index.html              # 入口文件，包含 CDN 引入与挂载点
├── css/
│   └── style.css           # 自定义全局样式（覆盖 Tailwind/ElementUI）
├── js/
│   ├── app.js              # Vue 实例初始化、路由配置
│   ├── router.js           # 路由定义（按角色拆分）
│   ├── store.js            # 简易状态管理（可选，或用 Vuex CDN）
│   ├── api.js              # AJAX/Fetch 请求封装
│   ├── components/         # Vue 组件
│   │   ├── Header.vue.js   # 顶部导航
│   │   ├── ProjectTable.js # 项目填报表格
│   │   └── WipChart.js     # WIP 分析图表
│   └── views/              # 页面视图
│       ├── Dashboard.js    # 数据看板
│       ├── Editor.js       # 填报页面
│       └── Approval.js     # 审批页面
└── assets/
    └── icons/              # 静态 SVG/图片资源
```

---

## 五、注意事项

1. **Tailwind CDN 限制：** Play CDN 版本会在浏览器中实时编译 CSS，生产环境建议替换为预编译的 Tailwind CSS 文件，或使用 PurgeCSS 清理无用类名。
2. **Vue Devtools：** 由于使用 CDN 引入的 `vue.js`（非 min 版），浏览器可正常安装 Vue Devtools 插件进行调试。
3. **ElementUI 版本兼容：** 确保 ElementUI 版本与 Vue 2.5.22 兼容。若遇到报错，可尝试升级 Vue 至 `2.6.x` 或降级 ElementUI。
4. **ECharts 按需引入：** CDN 引入的是全量 `echarts.min.js` (~1MB)。若后续体积敏感，可考虑使用 ECharts 的按需构建版本。
5. **Luckysheet 权限控制：** 利用 Luckysheet 的 `cellRightClickConfig` 和单元格属性（`status`, `locked`）实现不同角色的可见/可编辑权限。建议在初始化 `luckysheet.create()` 时传入动态生成的权限配置。
6. **Luckysheet 数据交互：** Luckysheet 内部维护二维数组状态。填报提交时，通过 `luckysheet.getAllSheets()` 获取 JSON 数据并序列化发送给后端；回填时需将后端 JSON 转换为 Luckysheet 的 Sheet 配置格式。
